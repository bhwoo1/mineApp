/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ssl.pstatic.net'],
        domains: ['phinf.pstatic.net']// 사용할 호스트를 여기에 추가합니다.
      },
};

export default nextConfig;
